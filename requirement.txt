# ==========================================
# Crane AI Pipeline - 核心依赖清单 (v2.4+)
# ==========================================

# 1. PDF 与图像处理引擎 (对应 import fitz)
# 核心作用：PDF拆页、文本提取、高清图像渲染落盘
PyMuPDF>=1.23.0

# 2. 大模型通信 SDK
# 核心作用：与 Qwen3-VL (兼容 OpenAI 格式) 网关进行异步高并发通信
openai>=1.14.0

# 3. 环境变量与安全管理
# 核心作用：加载 .env 文件，防止数据库密码和 API Key 泄露
python-dotenv>=1.0.0

# 4. 策略与配置大脑
# 核心作用：解析 rules.yaml 策略配置文件
PyYAML>=6.0.1

# 5. 数据校验与配置管理 (强烈建议)
# 核心作用：在 v2.3+ 中用于 Quality Gate (质量门禁) 的强类型数据约束
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ==========================================
# 6. 数据库驱动 (根据实际情况取消注释)
# ==========================================
# 核心作用：对接达梦数据库 (DMDB)。
# 注意：你需要根据你的 Python 环境和达梦官方文档安装对应的驱动。
# dmPython>=2.4.5 

# ==========================================
# 7. 进阶/可选依赖 (预留给 v2.5 的性能极致优化)
# ==========================================
# 如果你后续要加入 Layout Analysis (版面预裁剪) 和 高级图像操作，可以安装以下库：
# Pillow>=10.2.0        # 图像裁剪与处理
# paddlepaddle          # 百度 Paddle 深度学习框架 (用于本地端极速跑 PP-Structure)
# paddleocr             # 极速表格与版面检测
gradio_client>=1.8.0